diff --git a/PARPACK/TESTS/MPI/icb_parpack_c.c b/PARPACK/TESTS/MPI/icb_parpack_c.c
index e19065e..2d440de 100644
--- a/PARPACK/TESTS/MPI/icb_parpack_c.c
+++ b/PARPACK/TESTS/MPI/icb_parpack_c.c
@@ -28,12 +28,12 @@ void dMatVec(int start, int N, const double* x, double* y) {
 };
 
 int ds() {
-  const a_int N      = 1000;
-  const a_int nev    = 9;
-  const a_int ncv    = 2 * nev + 1;
-  const a_int ldv    = N;
-  const a_int ldz    = N;
-  const a_int lworkl = ncv * (ncv + 8);
+  enum { N = 1000 };
+  enum { nev = 9 };
+  enum { ncv = 2 * nev + 1 };
+  enum { ldv = N };
+  enum { ldz = N };
+  enum { lworkl = ncv * (ncv + 8) };
   const a_int rvec   = 1;        // need eigenvectors
 
   const double tol   = 0.000001; // small tol => more stable checks after EV computation.
@@ -104,16 +104,22 @@ int ds() {
 
 void zMatVec(int start, int N, const a_dcomplex* x, a_dcomplex* y) {
   int i;
-  for (i = 0; i < N; ++i) y[i] = x[i] * CMPLXF(start + i + 1.0, start + i + 1.0);
+  for (i = 0; i < N; ++i) {
+#if !defined(__INTEL_LLVM_COMPILER) && defined(_MSC_VER)
+    y[i] = _Cmulcc(x[i], CMPLX(start + i + 1.0, start + i + 1.0));
+#else
+    y[i] = x[i] * CMPLXF(start + i + 1.0, start + i + 1.0);
+#endif
+  }
 };
 
 int zn() {
-  const a_int N      = 1000;
-  const a_int nev    = 9;
-  const a_int ncv    = 2 * nev + 1;
-  const a_int ldv    = N;
-  const a_int ldz    = N;
-  const a_int lworkl = ncv * (3 * ncv + 5);
+  enum { N = 1000 };
+  enum { nev = 9 };
+  enum { ncv = 2 * nev + 1 };
+  enum { ldv = N };
+  enum { ldz = N };
+  enum { lworkl = ncv * (3 * ncv + 5) };
   const a_int rvec   = 0;                 // eigenvectors omitted
 
   const double tol = 0.000001;            // small tol => more stable checks after EV computation.
diff --git a/TESTS/bug_1315_double.c b/TESTS/bug_1315_double.c
index a1d69c7..c99428b 100644
--- a/TESTS/bug_1315_double.c
+++ b/TESTS/bug_1315_double.c
@@ -23,12 +23,12 @@ void matVec(double *x, double *y) {
 int main() {
   a_int ido = 0;
   char bmat[] = "I";
-  a_int N = 1000;
+  enum { N = 1000 };
   char which[] = "LM";
-  a_int nev = 9;
+  enum { nev = 9 };
   double tol = 0;
   double resid[N];
-  a_int ncv = 2 * nev + 1;
+  enum { ncv = 2 * nev + 1 };
   double V[ncv * N];
   a_int ldv = N;
   a_int iparam[11];
diff --git a/TESTS/bug_1315_single.c b/TESTS/bug_1315_single.c
index f7fc633..dcf4427 100644
--- a/TESTS/bug_1315_single.c
+++ b/TESTS/bug_1315_single.c
@@ -23,12 +23,12 @@ void matVec(float *x, float *y) {
 int main() {
   a_int ido = 0;
   char bmat[] = "I";
-  a_int N = 1000;
+  enum { N = 1000 };
   char which[] = "LM";
-  a_int nev = 9;
+  enum { nev = 9 };
   float tol = 0;
   float resid[N];
-  a_int ncv = 2 * nev + 1;
+  enum { ncv = 2 * nev + 1 };
   float V[ncv * N];
   a_int ldv = N;
   a_int iparam[11];
diff --git a/TESTS/icb_arpack_c.c b/TESTS/icb_arpack_c.c
index bdf78ce..46d74ce 100644
--- a/TESTS/icb_arpack_c.c
+++ b/TESTS/icb_arpack_c.c
@@ -27,12 +27,12 @@ void dMatVec(const double* x, double* y) {
 };
 
 int ds() {
-  const a_int N      = 1000;
-  const a_int nev    = 9;
-  const a_int ncv    = 2 * nev + 1;
-  const a_int ldv    = N;
-  const a_int ldz    = N;
-  const a_int lworkl = ncv * (ncv + 8);
+  enum { N = 1000 };
+  enum { nev = 9 };
+  enum { ncv = 2 * nev + 1 };
+  enum { ldv = N };
+  enum { ldz = N };
+  enum { lworkl = ncv * (ncv + 8) };
   const a_int rvec   = 1;      // need eigenvectors
 
   const double tol = 0.000001; // small tol => more stable checks after EV computation.
@@ -92,16 +92,22 @@ int ds() {
 
 void zMatVec(const a_dcomplex* x, a_dcomplex* y) {
   int i;
-  for (i = 0; i < 1000; ++i) y[i] = x[i] * CMPLX(i + 1.0, i + 1.0);
+  for (i = 0; i < 1000; ++i) {
+#if !defined(__INTEL_LLVM_COMPILER) && defined(_MSC_VER)
+    y[i] = _Cmulcc(x[i], CMPLX(i + 1.0, i + 1.0));
+#else
+    y[i] = x[i] * CMPLX(i + 1.0, i + 1.0);
+#endif
+  }
 };
 
 int zn() {
-  const a_int N      = 1000;
-  const a_int nev    = 9;
-  const a_int ncv    = 2 * nev + 1;
-  const a_int ldv    = N;
-  const a_int ldz    = N;
-  const a_int lworkl = ncv * (3 * ncv + 5);
+  enum { N = 1000 };
+  enum { nev = 9 };
+  enum { ncv = 2 * nev + 1 };
+  enum { ldv = N };
+  enum { ldz = N };
+  enum { lworkl = ncv * (3 * ncv + 5) };
   const a_int rvec   = 0;                 // eigenvectors omitted
 
   const double tol = 0.000001;            // small tol => more stable checks after EV computation.
