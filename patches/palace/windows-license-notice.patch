--- a/palace/main.cpp
+++ b/palace/main.cpp
@@ -2,6 +2,7 @@
 // SPDX-License-Identifier: Apache-2.0
 
 #include <filesystem>
+#include <iostream>
 #include <memory>
 #include <optional>
 #include <string>
@@ -155,6 +156,25 @@
 int main(int argc, char *argv[])
 #endif
 {
+#if defined(_WIN32)
+  // Keep third-party rights accessible from the executable interface (CeCILL-C 6.4).
+  if (argc == 2 && std::string_view(argv[1]) == "--licenses")
+  {
+    std::cout << "Palace Windows: Apache-2.0; see licenses/ and README.txt in the package.\n"
+                 "This software uses MUMPS 5.7.3 under CeCILL-C, with exceptions in its LICENSE.\n"
+                 "Copyright 1991-2024 CERFACS, CNRS, ENS Lyon, INP Toulouse, Inria,\n"
+                 "Mumps Technologies, University of Bordeaux.\n"
+                 "See licenses/mumps/ and sources/MUMPS_5.7.3.tar.gz for notices and source.\n"
+                 "MUMPS is provided without warranty, subject to its license liability limits.\n"
+                 "METIS uses attributed Apache-2.0 NetworkX-METIS sorting prior art.\n"
+                 "Copyright NetworkX Developers and Regents of the University of Minnesota.\n"
+                 "Excluded LGPL source remains identified in the source bundle; see licenses/metis/.\n"
+                 "Eigen covered source and other corresponding source are in sources/.\n"
+                 "Proprietary runtime terms apply only to their respective components.\n"
+                 "See RUNTIME_TERMS.txt and licenses/ for the complete component agreements.\n";
+    return 0;
+  }
+#endif
   // Initialize MPI.
 #if defined(MFEM_USE_STRUMPACK) && \
     (defined(STRUMPACK_USE_PTSCOTCH) || defined(STRUMPACK_USE_SLATE_SCALAPACK))
@@ -176,6 +196,9 @@
                "Options:\n"
                "  -h, --help           Show this help message and exit\n"
                "  --version            Show version information and exit\n"
+#if defined(_WIN32)
+               "  --licenses           Show third-party rights and license locations\n"
+#endif
                "  -dry-run, --dry-run  Parse configuration file for errors and exit\n\n",
                executable_path.substr(executable_path.find_last_of('/') + 1));
   };
