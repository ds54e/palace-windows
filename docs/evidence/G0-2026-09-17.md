# Gate 0 investigation — 2026-09-17

Status: blocked; no linked MPI probe, MPI execution, Palace build, or clean-host pass.

## Observed host

- WSL2 x86_64, kernel 6.18.33.2-microsoft-standard-WSL2; native Windows interop works.
- Native Windows reports 10.0.26200.0, 64-bit process.
- Windows token has medium integrity and Administrators marked deny-only. This is a non-elevated administrator account, not proof of a standard-user account.
- Codex CLI 0.154.0. Project configuration requests Astra/low. Effective model, effort, account access, and premium speed could not be independently verified through this session's tools; no nested CLI or child was launched.
- Visual Studio Build Tools 17.14.36623.8; installed MSVC directory 14.44.35207; Windows SDK library directory 10.0.22621.0.
- Bundled native CMake exists. CMake, Ninja, cl, ifx, Git and dumpbin were not found on the initial native PATH. Developer environment initialization succeeded.
- Neither common Intel oneAPI installation directory exists. This is a bounded search, not proof that no Fortran compiler exists anywhere.
- No System32 msmpi.dll, common MPI registry keys, or matching MPI services were observed. This limited inventory does not establish a clean host.
- Effective PowerShell execution policy is Restricted; all explicit scopes report Undefined. `bash tools/windows.sh doctor` failed with UnauthorizedAccess before running the script. No execution policy was changed or bypassed.

## Experiments and identities

1. `codex --version`, `uname -a`, `bash tools/windows.sh doctor`; raw failure: `.work/inventory/doctor.log`.
2. Direct `powershell.exe -NoLogo -NoProfile -NonInteractive -Command` read-only queries: Get-ExecutionPolicy, OSVersion, Is64BitProcess, whoami /groups, Get-Command, vswhere, Get-Service, Test-Path and Get-ItemProperty. Raw reports: `.work/inventory/native.txt`, `toolchain.txt`, `paths.txt`. These commands did not execute the blocked script body.
3. `git clone --filter=blob:none --no-checkout https://github.com/awslabs/palace.git .work/sources/palace`, then `git -C .work/sources/palace checkout --detach b92aef83ecfe6d360c4b3d83e2122986297f6778`. HEAD verified equal to the repository pin. Log: `.work/inventory/palace-fetch.log`.
4. `git clone --depth 1 https://github.com/microsoft/Microsoft-MPI.git .work/sources/msmpi`. Observed HEAD f2d849ff483b36add9bc2d18c6d7bbc9ce38f4ad. Log: `.work/inventory/msmpi-fetch.log`. Source investigation identity only, not a selected shipping runtime.
5. `cmd.exe /d /c 'E:\projects\palace-windows\.work\inventory\compile-probe.cmd'`. The local command file calls the installed vcvars64.bat, then `cl /nologo /c /EHsc /std:c++17 /W4 /permissive- /utf-8 /DNOMINMAX /DWIN32_LEAN_AND_MEAN`, with the Microsoft source include directory, repository `tests/runtime/mpi_singleton.cpp`, and local object output. Exit 0; log `.work/inventory/compile-probe.log`. This proves only native C++ compilation, not linking or MPI behavior. Script and object remain local.

## Source findings

- Palace `cmake/ExternalARPACK.cmake` requires MPI/PARPACK and explicitly uses the 32-bit integer interface with LP64 BLAS/LAPACK.
- Palace `cmake/ExternalMUMPS.cmake` selects parallel MUMPS, intsize64=OFF, ScaLAPACK, METIS and ParMETIS. A serial MUMPS recipe cannot be copied without compatibility investigation.
- Microsoft `src/mpi/pmilib/smpd_ipmi.cpp`, PMI_Init, has a missing-PMI_KVS path setting rank 0, size 1 and local KVS. This is source evidence for a singleton hypothesis, not runtime evidence.
- Microsoft source has MIT and MPICH notices. These do not automatically establish licensing for separately supplied vendor binaries.
- [WelSim discussion](https://github.com/WelSimLLC/WelSim-Apps/discussions/145) concerns Palace 0.15, VS 2022, Intel Fortran and MKL, and includes manual project changes. No patches were copied. Its recipe is not a reproducible or deployment-validated recipe for the pinned version.
- [Microsoft download page](https://www.microsoft.com/en-us/download/details.aspx?id=100593) lists 10.1.2 / 10.1.12498.18 and permits redistribution of installers subject to the license agreement. This does not settle extracted-DLL deployment rights. GitHub latest-release API returned v10.1.1; do not confuse these identities. Response retained at `.work/inventory/msmpi-release.json`.

## Next discriminating work / external prerequisites

- Owner must authorize an acceptable way to execute the repository PowerShell diagnostics on this host, or provide a host where they are permitted. The agent must not weaken execution policy under current authority.
- Establish an exact runtime acquisition and redistribution route before staging a payload: inspect version-specific vendor terms or investigate building Microsoft's existing source with its documented prerequisites. No arbitrary System32 DLL collection or replacement MPI is permitted.
- Then link the existing probe against the matching SDK, stage only reviewed runtime bytes, and run the wrapper to observe loaded-module path and singleton behavior.
- A separately prepared standard-user clean Windows environment with offline operation is still required. Do not attest this developer machine as clean.
- Before G1, locate or owner-provision a compatible Fortran toolchain; any privileged installation needs separate authorization.
